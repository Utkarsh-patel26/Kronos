// kronos-gui: JavaFX dashboard. Depends on common and server.
// JavaFX libraries are added in a later phase. This module is an empty skeleton for now.
dependencies {
    implementation(project(":kronos-common"))
    implementation(project(":kronos-server"))
}
